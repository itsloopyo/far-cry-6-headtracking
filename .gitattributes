* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
*.png binary
*.gif binary
*.zip binary
# Differential test inputs are byte-exact player files: CRLF, and whatever else a build wrote.
tests/config_differential/inputs/** -text
# The committed settings file is byte-exact: the render test compares it with the table.
config/FarCry6HeadTracking.ini -text
